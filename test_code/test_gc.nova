fn print_name(first, last)
    full := first + " " + last
    println("fullname = ", full)
end

first := "Tony"
last := "Stark"

num := 5

while num > 0
    print_name(first, last)
    num = num - 1
end